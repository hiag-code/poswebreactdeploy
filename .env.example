# Copie este arquivo para .env e preencha com a SUA senha local do PostgreSQL.
# O .env de verdade NÃO vai pro Git (está no .gitignore).
DATABASE_URL=postgresql://postgres:SUA_SENHA@localhost:5432/agents_db
